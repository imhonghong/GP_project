import csv
path=r"C:\python\sw\fourclass.csv"
with open(path, newline='') as csvfile:
    rows = csv.reader(csvfile)
    for row in rows:
        print(row)
rows_num=[list(map(float, row)) for row in rows]

